# Copy this file to .env and fill in your values.
# When running outside Docker, point directly at the backend server.
# When running inside Docker, this is set automatically via docker-compose.yml.
REACT_APP_API_URL=http://localhost:8000
